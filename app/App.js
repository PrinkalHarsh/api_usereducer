import React from 'react';
import {View} from 'react-native';
import {ApiCalling} from '@components';

const App = (post) => {
  return (
    <View>
      <ApiCalling />
    </View>
  );
};

export default App;
